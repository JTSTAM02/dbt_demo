version: 2

models:
  - name: dim_students
    description: "Student dimension derived from snapshot"
    columns:
      - name: student_key
        tests:
          - not_null
          - unique

      - name: student_id
        tests:
          - not_null

      - name: is_current
        tests:
          - not_null
